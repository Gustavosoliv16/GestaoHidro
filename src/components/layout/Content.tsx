import Dashboard from "../dashboard/Dashboard";

export default function Content() {
  return (
    <div className="layout-content fade-in" style={{ padding: 'var(--space-6)' }}>
      <Dashboard />
    </div>
  );
}
